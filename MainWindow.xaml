<Window x:Class="Safali.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="http://schemas.microsoft.com/winfx/2006/xaml/presentation/shell"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:tab="clr-namespace:Safali"
        xmlns:local="clr-namespace:Safali"
        mc:Ignorable="d"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        Title="MainWindow" Height="400" Width="600" Icon="/Safali.png" Closing="Window_Closing" MinWidth="600" MinHeight="400">
    <Window.Resources>
        <Style x:Key="TabItemContainerStyle" TargetType="{x:Type TabItem}">
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Label Content="{Binding}">
                            <Label.Style>
                                <Style TargetType="Label">
                                    <EventSetter Event="MouseEnter" Handler="TabItem_MouseEnter"/>
                                    <EventSetter Event="MouseLeave" Handler="TabItem_MouseLeave"/>
                                </Style>
                            </Label.Style>
                        </Label>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="23*"/>
            <ColumnDefinition Width="9*"/>
        </Grid.ColumnDefinitions>
        <TabControl x:Name="tabControl" ItemContainerStyle="{StaticResource TabItemContainerStyle}" Grid.ColumnSpan="2">
            <TabItem Header="tabItem1" Name="tabItem1">
                <Grid>
                    <cef:ChromiumWebBrowser
                x:Name="browser"
                Margin="0,0,0,0"/>
                </Grid>
            </TabItem>
            <TabItem Header="Tab 2"></TabItem>
            <TabItem Header="Tab 3"></TabItem>
        </TabControl>
    </Grid>
</Window>
